# ---------------------------------------------------------------------------
<<<<<<< HEAD
# contrat-service (EDUCA.TECH) - Example environment
# Copy to .env and adjust for your environment.
# ---------------------------------------------------------------------------

# --- Service ---
NODE_ENV=development
PORT=8091
SERVICE_NAME=contrat-service
LOG_LEVEL=info
# Public base domain (behind Kong)
PUBLIC_DOMAIN=contrat.educasoft.tech

# --- PostgreSQL ---
=======
# contrat-service (EDUCA.TECH) - Phase 1 - Exemple de configuration
# Copier vers .env et adapter. NE JAMAIS committer le vrai .env.
# ---------------------------------------------------------------------------

# Application
NODE_ENV=development
PORT=8091
SERVICE_NAME=contrat-service
API_PREFIX=/api/v1
LOG_LEVEL=info

# Base de donnees PostgreSQL (base dediee)
>>>>>>> 34a51170a14d49e3daadfd6c75baf0624cf970a3
DB_HOST=localhost
DB_PORT=5432
DB_NAME=educa_contrat_service
DB_USER=educa
<<<<<<< HEAD
DB_PASSWORD=educa
DB_POOL_MAX=10
DB_POOL_MIN=0
# The application connects as a NON-superuser role so RLS is enforced.
# (Superusers and table owners bypass RLS unless FORCE ROW LEVEL SECURITY is set.)

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
# TTL (seconds) for cached active-year lookups
ANNEE_SCOLAIRE_CACHE_TTL=3600

# --- RabbitMQ ---
RABBITMQ_URL=amqp://educa:educa@localhost:5672
RABBITMQ_EXCHANGE=educa.contracts
RABBITMQ_EXCHANGE_TYPE=topic

# --- JWT (issued by manage-account:8081) ---
JWT_SECRET=educa_access_secret_2026
JWT_ALGORITHM=HS256

# --- EDUCA system tenant (owner of ETABLISSEMENT / global contracts) ---
# Fixed UUID identifying the EDUCA/Lizay system tenant.
EDUCA_SYSTEM_TENANT_ID=00000000-0000-0000-0000-0000000ed0ca
# Roles allowed to create ETABLISSEMENT-scoped contracts (comma separated)
EDUCA_ADMIN_ROLES=SUPER_ADMIN,EDUCA_ADMIN

# --- Inter-service HTTP (base URLs behind Kong or direct) ---
ANNEE_SCOLAIRE_SERVICE_URL=http://localhost:8082
MANAGE_ACCOUNT_SERVICE_URL=http://localhost:8081
SIGNATURE_SERVICE_URL=http://localhost:8093

# --- File storage ---
UPLOAD_DIR=./data/uploads
MAX_UPLOAD_SIZE_MB=25
=======
DB_PASSWORD=educa_secret
DB_SSL=false
DB_POOL_MAX=10
DB_POOL_MIN=0

# Redis (contexte, cache permissions / annee scolaire)
REDIS_URL=redis://localhost:6379
REDIS_PREFIX=contrat:

# RabbitMQ (bus d'evenements de cycle de vie)
RABBITMQ_URL=amqp://educa:educa@localhost:5672
RABBITMQ_EXCHANGE=educa.contracts

# JWT (secret PARTAGE emis par manage-account) - claims camelCase
JWT_SECRET=educa_access_secret_2026

# Tenant systeme EDUCA (proprietaire des contrats d'etablissement / globaux)
EDUCA_SYSTEM_TENANT_ID=00000000-0000-0000-0000-000000000001

# Roles autorises a creer un contrat d'etablissement (scope ETABLISSEMENT)
EDUCA_ADMIN_ROLES=SUPER_ADMIN,EDUCA_ADMIN

# Roles autorises a creer/publier des modeles de contrat (templates)
TEMPLATE_MANAGER_ROLES=SCHOOL_ADMIN,RESP_RH,EDUCA_ADMIN,SUPER_ADMIN

# Stockage documentaire (disque local en Phase 1)
UPLOAD_DIR=./storage/uploads
MAX_UPLOAD_MB=25

# Services inter-microservices (clients HTTP)
# En dev local: localhost:<port> ; en Docker: nom de conteneur sur educa-net.
# years-service (route reelle: GET /academic-years/active)
ACADEMIC_YEAR_SERVICE_URL=http://localhost:8082
# signature-service (GET /api/signatures, POST /api/signature-usages)
SIGNATURE_SERVICE_URL=http://localhost:8093
COMMUNICATION_SERVICE_URL=http://localhost:8087
# ecole-service (GET /api/schools/by-id/:id)
ECOLE_SERVICE_URL=http://localhost:3001
# enseignant/personnel-service (GET /api/teachers/:id)
ENSEIGNANT_SERVICE_URL=http://localhost:8083
# affectations-academiques-service (GET /api/affectations/enseignants/:id)
AFFECTATION_SERVICE_URL=http://localhost:3006
# manage-account (GET /api/users/:id)
MANAGE_ACCOUNT_SERVICE_URL=http://localhost:8081
# Optionnels (peuvent ne pas exister -> fallback local, laisser vide si absents)
RH_SERVICE_URL=
CONFIGURATION_SERVICE_URL=

# Cache Redis (secondes) des donnees stables inter-services
AGGREGATION_CACHE_TTL=300
>>>>>>> 34a51170a14d49e3daadfd6c75baf0624cf970a3
